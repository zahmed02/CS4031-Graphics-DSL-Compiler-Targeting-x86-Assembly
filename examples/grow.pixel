size = 1
dx = 1
x = 20
y = 12
loop 40 times
    clear_screen
    draw_rect x, y, size, size, 15
    size = size + dx
    if size > 10 then
        dx = -1
    end
    if size < 1 then
        dx = 1
    end
    delay 100
end
wait_key